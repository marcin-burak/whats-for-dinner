import { Routes } from '@angular/router';
import { MePageComponent } from './me-page/me-page.component';

export const routes: Routes = [
    { title: "What's for dinner | Me", path: "", component: MePageComponent }
];
